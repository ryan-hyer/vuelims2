<template>
  <div>
    Once data is entered, deliverables will be generated here. That is, one or more reports. I
    envision a button saying Generate Report or similar. Hitting the button will generate a PDF
    report from a template, with all the appropriate data entered automatically. Once generated, it
    should show up in a list to be retrieved again if desired. Changes to data should reset the
    generation logic and allow generation of a new revised report, which will then be added to the
    list. If the data hasn't been entered yet, then a message should appear saying that, instead of
    the button.
  </div>
</template>
