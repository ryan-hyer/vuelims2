{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path'); if echo \"$file\" | grep -qE '\\.(ts|vue|js|mjs)$'; then npx eslint \"$file\"; fi",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
